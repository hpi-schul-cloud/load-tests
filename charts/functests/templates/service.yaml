apiVersion: v1
kind: Service
metadata:
  name: {{ include "functests.fullname" . }}
  labels:
    {{- include "functests.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "functests.selectorLabels" . | nindent 4 }}
