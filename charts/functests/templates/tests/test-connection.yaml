apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "functests.fullname" . }}-test-connection"
  labels:
    {{- include "functests.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "functests.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
